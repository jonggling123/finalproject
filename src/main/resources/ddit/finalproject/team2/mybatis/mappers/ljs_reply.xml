<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ddit.finalproject.team2.common.dao.Ljs_IReplyDao">
	<select id="selectReplyCount" parameterType="string" resultType="int">
		SELECT COUNT(*)
		FROM REPLY
		WHERE BOARD_NO = #{board_no}
	</select>
	
	<select id="selectReplyList" parameterType="string" resultType="ddit.finalproject.team2.vo.Ljs_ReplyVo">
		SELECT REPLY_NO, A.USER_ID
			, REPLY_CONTENT, REPLY_REG
		FROM REPLY R INNER JOIN ATTEND A
		ON(R.ATTEND_NO = A.ATTEND_NO)
		WHERE BOARD_NO = #{board_no}
	</select>
	
	<insert id="insertReply" parameterType="ddit.finalproject.team2.vo.Ljs_ReplyVo">
		INSERT INTO REPLY(
			BOARD_NO, LECTURE_CODE, REPLY_NO, ATTEND_NO
			, REPLY_CONTENT, REPLY_REG
		) VALUES (
			#{board_no}, #{lecture_code}, REPLY_SEQ.NEXTVAL, #{attend_no}
			, #{reply_content}, SYSDATE
		)
	</insert>
	
	<delete id="deleteReply" parameterType="string">
		DELETE FROM REPLY
		WHERE REPLY_NO = #{reply_no}
	</delete>
	
	<delete id="deleteAllReply" parameterType="string">
		DELETE FROM REPLY
		WHERE BOARD_NO = #{board_no}
	</delete>
	
	<update id="updateReply" parameterType="string">
		UPDATE REPLY SET REPLY_CONTENT = 
	</update>
</mapper>